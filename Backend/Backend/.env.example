# .env.example - plantilla (NO subir a git)
# Rellenar en la instancia y proteger con: chmod 600 .env

DB_URL=
DB_USERNAME=
DB_PASSWORD=
# URL pública del modelo (no es secreto); opcional dejar en blanco y usar el default de application.properties
MODEL_API_URL=https://techmind-hackathon.duckdns.org/api/contenido
SERVER_PORT=8081
